const modelSuratMasuk = require('../models/surat_masuk')
// const { now } = require('sequelize/types/utils');
    or,
    where
} = require('sequelize');
const Sequelize = require('sequelize');
const controller = {}



const lihatsuratmasuk = async (req, res) => {
    try {
        const id = 1
        
    } catch (error) {
        res.status(500).json({
            success: false,
            message: error
        })

    }
}